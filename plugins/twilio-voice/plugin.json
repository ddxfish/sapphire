{
  "name": "twilio-voice",
  "short_display_name": "Twilio Voice",
  "version": "0.3.0",
  "description": "Sapphire answers real phone calls over Twilio SIP — no open ports (outbound SIP registration). Multi-account; per-number on/off via Triggers > Daemon.",
  "author": "Sapphire Trinity",
  "icon": "📞",
  "priority": 50,
  "default_enabled": true,
  "capabilities": {
    "tools": ["tools/phone_tool.py", "tools/elevate_tool.py"],
    "settings": [
      {"key": "call_cues", "type": "boolean", "label": "Turn cue sounds on calls", "default": true,
       "help": "Soft chimes on the line: a gentle 1/s pulse while she's thinking, a quiet ding when you barge in, and a goodbye chime before hangup."},
      {"key": "vad_threshold", "type": "number", "label": "Call VAD speech threshold", "default": 0.6,
       "help": "Phone lines are noisier than a desk mic — 0.6 rejects most line noise. 0 = inherit Settings > Conversation."},
      {"key": "endpoint_silence_ms", "type": "number", "label": "Call end-of-speech silence (ms)", "default": 0,
       "help": "Pause before she decides the caller is done. 0 = inherit Settings > Conversation (700)."},
      {"key": "min_speech_ms", "type": "number", "label": "Call minimum utterance (ms)", "default": 0,
       "help": "Speech shorter than this is discarded as a blip. 0 = inherit Settings > Conversation (200)."},
      {"key": "barge_hold_ms", "type": "number", "label": "Call barge-in hold (ms)", "default": 200,
       "help": "Sustained caller speech before it cuts her off mid-reply. Phone default 200 so line noise doesn't clip her. 0 = inherit (90)."},
      {"key": "max_utterance_ms", "type": "number", "label": "Call utterance hard cap (ms)", "default": 0,
       "help": "Backstop when noise pins the VAD open: force the turn after this long. 0 = inherit engine default (30000). Try 15000-20000 on noisy lines."}
    ],
    "scopes": [
      {
        "key": "twilio",
        "label": "twilio number",
        "endpoint": "/api/plugin/twilio-voice/accounts",
        "data_key": "accounts",
        "value_field": "scope",
        "name_field": "scope",
        "label_template": "{label}"
      }
    ],
    "daemon": {
      "entry": "daemon.py",
      "event_sources": [
        {
          "name": "incoming_call",
          "label": "Answer Phone Calls",
          "description": "Gate: when enabled, Sapphire answers calls on the selected Twilio number and converses live. This task is an on/off switch — it does not fire a one-shot reply.",
          "realtime": true,
          "filter_fields": [
            {"key": "caller", "label": "Caller number (allowlist)"}
          ],
          "task_fields": [
            {
              "key": "account",
              "type": "select",
              "label": "Twilio number",
              "required": true,
              "dynamic": "/api/plugin/twilio-voice/accounts",
              "help": "Which Twilio number this switch answers. Manage numbers in the plugin's settings."
            },
            {
              "key": "ephemeral",
              "type": "boolean",
              "label": "Ephemeral per-caller chat",
              "default": false,
              "help": "ON: each caller gets a fresh throwaway chat (📞) that auto-clears. OFF: use the saved Chat you pick in AI settings (persistent — she remembers across calls)."
            },
            {
              "key": "ephemeral_minutes",
              "type": "number",
              "label": "Keep ephemeral chat for (minutes)",
              "default": 10,
              "help": "How long a caller's throwaway chat survives after their last call. A call-back within this window resumes; after it, the chat is deleted. 0 = fresh every call."
            }
          ]
        },
        {
          "name": "call_ended",
          "label": "Phone Call Ended",
          "description": "Fires after a call finishes — use to summarize the call to memory, notify you, etc.",
          "filter_fields": [
            {"key": "caller", "label": "Caller number"},
            {"key": "account", "label": "Twilio number"},
            {"key": "reason", "label": "End reason"}
          ]
        }
      ]
    },
    "routes": [
      {"method": "GET", "path": "accounts", "handler": "routes/accounts.py:list_accounts"},
      {"method": "POST", "path": "accounts", "handler": "routes/accounts.py:save_account"},
      {"method": "DELETE", "path": "accounts/{scope}", "handler": "routes/accounts.py:delete_account"}
    ],
    "hooks": {
      "ghost_inject": "hooks/phone_context.py",
      "post_chat": "hooks/hangup_sentinel.py"
    }
  }
}
