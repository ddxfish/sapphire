# Core API and Web Interface
# Pin fastapi<1.0 to avoid breaking Starlette changes
# FastAPI manages its own starlette dep — don't install starlette separately
fastapi>=0.109.0,<1.0.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
jinja2>=3.1.0
itsdangerous>=2.1.0
cryptography>=41.0.0

# LLM API Client — <3: see root requirements.txt (2026-09-20 release check)
openai>=1.12.0,<3.0.0

# Audio Processing (wakeword, TTS, STT, local playback)
# numpy<3 — prep for the eventual numpy 3 ABI break the way numpy 2 bit
# users in April 2026. See root requirements.txt for full incident note.
numpy>=1.24.0,<3.0.0
sounddevice>=0.4.6
soundfile>=0.12.1

# Tokenization
tiktoken>=0.5.0

# HTTP and Web, for functions that use web
requests>=2.31.0
beautifulsoup4>=4.12.0
bcrypt>=4.0.0
PySocks>=1.7.1

# Anthropic Claude API (optional - only needed if using Claude provider) — <1: see root
anthropic>=0.39.0,<1.0.0

# Image resize for LLM
Pillow>=10.1.0

# PDF parsing
pypdf>=4.0.0

# Cron schedule parsing
croniter>=1.3.0

# Bitcoin wallet
bit>=0.8.0

# Timezone data (required on Windows, bundled on Linux/macOS)
tzdata>=2024.1

# Timezone auto-detect on Windows (sapphire.py soft-imports it)
tzlocal>=5.0

# Async HTTP (embeddings, STT/TTS providers); [socks]=socksio for SOCKS-for-all routing
httpx[socks]>=0.27.0

# EPUB parsing (Library / Knowledge v3)
ebooklib>=0.20

# HEIC/HEIF decode for Library images (iPhone default format)
pillow-heif>=0.16.0

# Telegram client (Telegram plugin)
telethon>=1.34.0

# MCP client (Model Context Protocol plugin) — <2: see root requirements.txt
mcp>=1.26.0,<2.0.0
